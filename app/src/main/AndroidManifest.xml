<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.storetodoor">

    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="true" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".OrdersLocation"></activity>
        <activity android:name=".OrderHistory" />
        <activity android:name=".CurrentOrders" />
        <activity android:name=".PlaceOrder" />
        <activity android:name=".MessengersLocation" />
        <activity android:name=".MessengerPanel" />
        <activity android:name=".MessengerRegistration" />
        <activity android:name=".ProductRegistration" />
        <activity android:name=".CustomerPanel" />
        <activity android:name=".ManagerPanel" />
        <activity
            android:name=".CustomerRegistration"
            android:label="@string/title_activity_customer_registration"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>